import 'package:flutter/material.dart';

class FocuseTab extends StatelessWidget {
  static  const String routeName = "focuse";
  const FocuseTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
    );
  }
}
